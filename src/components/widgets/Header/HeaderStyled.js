import styled from 'styled-components';
import { HeaderText } from 'components/controls';

const HeaderStyled = styled(HeaderText)`
  padding: 0.5%;
  float: left;
  display: inline;
`;

export default HeaderStyled;
